<head>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
        {{! <link href="/css/about.css" rel="stylesheet"> }}
<link href="/css/dashboard.css" rel="stylesheet">
</head>
{{> header}}
<body>
    <main class="main-content">
    {{> admin_views/admin_sidebar}}
            <div class="content">
            <div class="container mt-5">
        <h2>Session created successfully: </h2>
        <p><strong>Session Name:</strong> {{ sessionName }}</p>
        <p><strong>Date:</strong> {{ date }}</p>
        <p><strong>Start Time:</strong> {{ startTime }}</p>
        <p><strong>End Time:</strong> {{ endTime }}</p>
        <p><strong>Category:</strong> {{ category }}</p>
        <p><strong>Venue:</strong> {{ venue }}</p>

        <!-- Buttons to view all sessions and edit current session -->
        <div class="mt-3">
            <a href="/admin_sessions" class="btn btn-primary">View All Sessions</a>
            <a href="/edit_session/{{ sessionId }}" class="btn btn-secondary">Edit Session</a>
        </div>
    </div>
        </div>
        <!-- Your dynamic content goes here -->
    </main>
</body>
{{> footer}}
